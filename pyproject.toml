[project]
name = "minigrad"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
    "tqdm>=4.66",
]
